[build]
builder = "nixpacks"
watchPatterns = ["main.py", "modules/**", "requirements.txt", "Procfile", ".env"]

[deploy]
startCommand = "python main.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
