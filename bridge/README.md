# Bridge

Shared task queue between Computer 1 and Computer 2.

- `tasks.md` — active task list, both machines read/write
- Push to git after updating so the other machine can pull

Both machines should have this repo cloned. If not, Kalani copy-pastes between them.
